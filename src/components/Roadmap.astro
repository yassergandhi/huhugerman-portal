---
const weeks = Array.from({ length: 10 }, (_, i) => ({
  number: i + 1,
  status: i === 0 ? 'available' : 'locked'
}));
---
<section class="roadmap-section">
  <h2>Ruta de Aprendizaje · 10 Semanas</h2>
  <p class="site-subtitle">
    Cada semana refuerza lo visto en clase los lunes y miércoles.
  </p>
  
  <ul class="roadmap-list">
    {weeks.map(week => (
      <li class="roadmap-item">
        <span>Semana {week.number.toString().padStart(2, '0')}</span>
        <span class={`status-badge status-${week.status}`}>
          {week.status === 'available' ? 'Disponible' : 'Próximamente'}
        </span>
      </li>
    ))}
  </ul>
</section>
